{%- assign current_lang = 'tags.lang' | t | append: '|' -%}{%- for tag in product.tags -%}{%- if tag contains current_lang -%} {%- assign lang_tag = tag | split: '|' | last -%}{%- assign tag_split = lang_tag | split: '_' -%}{%- if tag_split.first == 'name' -%}{%- assign color_no = tag_split | last | split: ':' | first -%}{%- assign color_name = tag_split | last | split: ':' | last -%}{%- if get == 'name' -%}{{color_name}}{%- elsif get == 'no' -%}{{color_no}}{%- endif -%}{%- break -%}{%- endif -%}{%- endif -%}{%- endfor -%}